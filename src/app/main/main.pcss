
.main-header {
    position: relative;
    height: $nav-height;
    align-items: center;
    background-color: #1f1f1f;
}

.main-header > .header-title {
    position: absolute;
    left: 0;
    margin: 1rem;
    font-size: 1.5rem;
}

.main-header > .header-login {
    position: absolute;
    right: 0;
    margin: 1rem;
    font-size: 1.5rem;
    text-align: right;
}
